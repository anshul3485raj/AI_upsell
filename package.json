{
  "name": "ai_upsell_root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "extensions/ai-upsell-widget"
  ],
  "scripts": {
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend && shopify app build"
  },
  "devDependencies": {},
  "dependencies": {}
}
